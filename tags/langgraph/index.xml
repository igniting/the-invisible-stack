<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>LangGraph on The Harness</title><link>https://igniting.github.io/the-invisible-stack/tags/langgraph/</link><description>Recent content in LangGraph on The Harness</description><generator>Hugo -- gohugo.io</generator><language>en-us</language><lastBuildDate>Thu, 02 Apr 2026 00:00:00 +0000</lastBuildDate><atom:link href="https://igniting.github.io/the-invisible-stack/tags/langgraph/index.xml" rel="self" type="application/rss+xml"/><item><title>Chapter 2: The Chain, the Graph, and the Loop</title><link>https://igniting.github.io/the-invisible-stack/book/chapter-02-the-chain-the-graph-and-the-loop/</link><pubDate>Thu, 02 Apr 2026 00:00:00 +0000</pubDate><guid>https://igniting.github.io/the-invisible-stack/book/chapter-02-the-chain-the-graph-and-the-loop/</guid><description>&lt;h2 id="three-metaphors-for-how-agents-think--and-why-each-one-broke"&gt;&lt;a href="#three-metaphors-for-how-agents-think--and-why-each-one-broke" class="header-anchor"&gt;&lt;/a&gt;Three metaphors for how agents think — and why each one broke
&lt;/h2&gt;&lt;hr&gt;
&lt;h2 id="act-1-the-story"&gt;&lt;a href="#act-1-the-story" class="header-anchor"&gt;&lt;/a&gt;Act 1: The Story
&lt;/h2&gt;&lt;p&gt;Six months into building his company&amp;rsquo;s agent platform, Sam Kovic hit the wall.&lt;/p&gt;
&lt;p&gt;It was a Thursday afternoon in August 2023. He was in a conference room with two of his engineers, staring at a whiteboard covered in arrows, and they were trying to debug an agent that processed customer support tickets. The agent was supposed to read the ticket, search the knowledge base for relevant documentation, draft a response, check the draft against company policy, and send it. Five steps. A chain.&lt;/p&gt;
&lt;p&gt;The problem was that tickets didn&amp;rsquo;t fit neatly into five steps. Some tickets required the agent to ask the customer a clarifying question before searching the knowledge base. Some required checking the customer&amp;rsquo;s account status before drafting a response, which meant calling an API that sometimes timed out, which meant the agent needed retry logic, which meant it needed to remember where it was when the retry succeeded. Some tickets mentioned multiple issues — a billing question and a bug report in the same message — and the agent needed to split them, route each part to the right handler, and merge the responses back into a single reply.&lt;/p&gt;
&lt;p&gt;Each of these variations was, in LangChain&amp;rsquo;s chain abstraction, a separate chain. A chain for tickets with clarifying questions. A chain for tickets that needed account lookups. A chain for tickets with multiple issues. Sam&amp;rsquo;s team had written fourteen different chains for what was, from the customer&amp;rsquo;s perspective, a single interaction: &amp;ldquo;I sent a support ticket and got a reply.&amp;rdquo;&lt;/p&gt;
&lt;p&gt;The chains shared code, sort of. They diverged in places that were hard to predict. When a bug appeared — and bugs appeared constantly, because language models are non-deterministic and fourteen code paths are fourteen opportunities for things to go wrong — debugging meant first figuring out which of the fourteen chains the ticket had been routed to, then finding the specific step where the model had made a wrong turn, then understanding what prompt had been constructed at that step, which required reading LangChain&amp;rsquo;s source code because the prompts were embedded inside the framework&amp;rsquo;s abstractions, not in Sam&amp;rsquo;s code.&lt;/p&gt;
&lt;p&gt;&amp;ldquo;I&amp;rsquo;m spending more time reading LangChain&amp;rsquo;s internals than writing our own product,&amp;rdquo; said one of his engineers, a statement that would have been funny if it weren&amp;rsquo;t also true.&lt;/p&gt;
&lt;p&gt;Sam walked to the whiteboard. He erased the arrows and drew a picture that was not a chain. It was a graph — nodes for each operation (read ticket, search knowledge base, check account, draft response, check policy, send), with edges between them that could go forward, backward, or sideways depending on the situation. Some edges were conditional: if the ticket mentions billing, go to the billing handler; if it mentions a bug, go to the technical handler. One edge looped back: if the policy check fails, go back to draft response and try again. One edge went off the board entirely: if the agent has been looping for more than three attempts, escalate to a human.&lt;/p&gt;
&lt;p&gt;He stepped back and looked at what he&amp;rsquo;d drawn. It was not a chain. It was a state machine — a system with defined states and explicit transitions between them. The ticket moved through the states based on its content and the results of each step, not based on a fixed sequence that some developer had prescribed in advance.&lt;/p&gt;
&lt;p&gt;&amp;ldquo;This is what the agent should look like,&amp;rdquo; Sam said. &amp;ldquo;But LangChain can&amp;rsquo;t express this. At least not without building a state machine on top of it by hand.&amp;rdquo;&lt;/p&gt;
&lt;p&gt;The irony was that Harrison Chase — LangChain&amp;rsquo;s creator — had already arrived at the same conclusion.&lt;/p&gt;
&lt;hr&gt;
&lt;p&gt;Chase had been hearing the same complaints from thousands of developers, and the complaints converged on the same theme Sam had identified: the chain abstraction assumed that agent work was linear, and real agent work was almost never linear.&lt;/p&gt;
&lt;p&gt;The criticism came from multiple directions. Some developers complained about opacity — the framework&amp;rsquo;s high-level abstractions hid the prompts being sent to the model, making debugging a forensic exercise. Some complained about control — they wanted to add custom logic between steps, inject human approval before certain actions, implement their own error handling, and the chain abstraction made none of this easy. Some complained about bloat — LangChain had grown into a massive package with hundreds of integrations, each adding dependencies and surface area for bugs.&lt;/p&gt;
&lt;p&gt;But the deeper issue, the one that no amount of engineering polish could fix, was structural. A chain could only go forward. Step one to step two to step three. If step three needed to loop back to step one, or branch to step four-A or step four-B depending on a condition, or pause for human input and resume later, the developer had to build that logic on top of the chain — effectively building a state machine using a tool designed for pipelines. It was possible but perverse, like driving screws with a hammer.&lt;/p&gt;
&lt;p&gt;In the summer of 2023, Chase made a decision that Sam would later describe to Elena as &amp;ldquo;the bravest thing I&amp;rsquo;ve seen a founder do.&amp;rdquo; He began building a replacement for his own creation. Not a patch, not a version 2.0 — a genuinely different framework with a genuinely different abstraction. He called it LangGraph.&lt;/p&gt;
&lt;p&gt;LangGraph launched in January 2024. Sam adopted it within weeks.&lt;/p&gt;
&lt;hr&gt;
&lt;p&gt;The difference was immediate. Where LangChain had chains, LangGraph had a &lt;strong&gt;StateGraph&lt;/strong&gt; — a directed graph where nodes were functions and edges defined control flow. But the real innovation was not the graph. It was the explicit treatment of &lt;strong&gt;state&lt;/strong&gt;.&lt;/p&gt;
&lt;p&gt;In a chain, state was implicit. The output of one step became the input to the next, and that was the extent of the system&amp;rsquo;s memory. If step three needed information from step one that wasn&amp;rsquo;t in step two&amp;rsquo;s output, you had to thread it through by hand — passing data along like a bucket brigade, each link responsible for carrying information it didn&amp;rsquo;t need for the sole purpose of handing it to a later link.&lt;/p&gt;
&lt;p&gt;In LangGraph, state was a first-class object. You defined a typed data structure — Sam used a Python TypedDict containing the ticket text, the customer ID, the conversation history, the retrieved documents, and a retry counter — and every node received the current state, did its work, and returned a partial update. The framework merged the update into the existing state. The state was the single source of truth for everything the agent knew and everything it had done.&lt;/p&gt;
&lt;p&gt;This made three things possible that changed how Sam thought about agent architecture.&lt;/p&gt;
&lt;p&gt;The first was &lt;strong&gt;checkpointing&lt;/strong&gt;. Because the state was an explicit, serializable object, the framework could save it to a database after each node. If the process crashed — due to an error, a timeout, a deployment, or an infrastructure outage — a new process could load the most recent checkpoint and resume from that point. The agent didn&amp;rsquo;t re-execute the nodes that had already completed. The tool calls that had already been made were not repeated. The emails that had already been sent were not sent again.&lt;/p&gt;
&lt;p&gt;Sam had been losing sleep over this problem. His chain-based agents ran in a single process, and if that process died, the entire interaction had to be re-run from the beginning. For a quick two-step lookup, restarting was fine. For a ten-minute agent session that had already modified customer records, sent notifications, and filed internal reports, restarting was somewhere between wasteful and catastrophic. Checkpointing turned agent execution from a fragile, all-or-nothing gamble into a durable, resumable process.&lt;/p&gt;
&lt;p&gt;The second was &lt;strong&gt;human-in-the-loop&lt;/strong&gt;. Sam could now define certain nodes in his graph as &lt;strong&gt;interrupt points&lt;/strong&gt;. When the agent reached an interrupt node — say, before sending a response to a customer, or before modifying a billing record — the framework paused execution, serialized the state, and emitted it to whatever system was responsible for collecting human input. A Slack notification, an email, a dashboard widget. The human reviewed the agent&amp;rsquo;s proposed action, approved or modified it, and the framework resumed the graph from the interrupt point with the human&amp;rsquo;s input merged into the state.&lt;/p&gt;
&lt;p&gt;This was not a feature Sam bolted onto the side of his system. It was a natural consequence of the graph-and-state architecture. The framework didn&amp;rsquo;t distinguish between &amp;ldquo;the model decided what to do next&amp;rdquo; and &amp;ldquo;the human decided what to do next.&amp;rdquo; Both were state transitions. Both were checkpointed. Both were logged. The uniformity was elegant, and it solved a problem that had been nagging Sam since his conversation with Marcus Chen at the security conference.&lt;/p&gt;
&lt;p&gt;&amp;ldquo;How do you decide where to put the interrupts?&amp;rdquo; Marcus asked when Sam showed him the architecture.&lt;/p&gt;
&lt;p&gt;&amp;ldquo;Where the cost of being wrong is highest,&amp;rdquo; Sam said. &amp;ldquo;Before sending external communications. Before modifying financial records. Before any action that can&amp;rsquo;t be undone.&amp;rdquo;&lt;/p&gt;
&lt;p&gt;&amp;ldquo;And the rest of the time, the agent just runs?&amp;rdquo;&lt;/p&gt;
&lt;p&gt;&amp;ldquo;The rest of the time, the agent just runs.&amp;rdquo;&lt;/p&gt;
&lt;p&gt;Marcus considered this. &amp;ldquo;That&amp;rsquo;s the right design. Most people either approve everything — which makes the agent useless — or approve nothing, which makes it dangerous. You&amp;rsquo;re approving the things that matter.&amp;rdquo;&lt;/p&gt;
&lt;p&gt;The third was &lt;strong&gt;streaming&lt;/strong&gt;. Because the graph&amp;rsquo;s execution was a series of discrete, observable node transitions, each transition could be emitted to the client in real time. Sam built a dashboard that showed the agent&amp;rsquo;s progress as it moved through the graph: &amp;ldquo;Reading ticket… Searching knowledge base… Found 3 relevant documents… Drafting response… Checking against policy… Policy check passed… Awaiting human approval.&amp;rdquo; The customer support team could watch the agent work, understand what it was doing and why, and catch problems before they reached the customer.&lt;/p&gt;
&lt;p&gt;This transparency turned out to be as important for internal trust as it was for debugging. Sam&amp;rsquo;s CEO, Leo, had been skeptical of the agent&amp;rsquo;s judgment. The streaming dashboard changed his mind. Not because the agent stopped hallucinating, but because when it did hallucinate, the dashboard made the failure visible and understandable. Leo could see which step had gone wrong and why. The agent was no longer a black box. It was a process with visible, auditable steps.&lt;/p&gt;
&lt;p&gt;&amp;ldquo;I trust this more,&amp;rdquo; Leo told Sam after watching the dashboard for a week. &amp;ldquo;Not because it&amp;rsquo;s perfect. Because I can see when it&amp;rsquo;s not.&amp;rdquo;&lt;/p&gt;
&lt;hr&gt;
&lt;p&gt;While Sam was rebuilding his system on LangGraph, Elena Voss was having a very different experience.&lt;/p&gt;
&lt;p&gt;Elena&amp;rsquo;s startup had chosen CrewAI — a multi-agent framework that had launched in late 2023 with a compelling pitch: define your agents as characters with roles, goals, and backstories, and let them collaborate on tasks like a human team. A &amp;ldquo;researcher&amp;rdquo; agent that gathered information. A &amp;ldquo;writer&amp;rdquo; agent that drafted responses. A &amp;ldquo;reviewer&amp;rdquo; agent that checked quality. The metaphor was intuitive and the getting-started experience was superb. Elena had a working prototype in four days.&lt;/p&gt;
&lt;p&gt;CrewAI was not a chain or a graph. It was closer to the &lt;strong&gt;loop&lt;/strong&gt; metaphor — each agent ran in a cycle of reasoning and action — but with a multi-agent layer on top: the agents communicated with each other, handed off tasks, and coordinated their work through a shared context. Elena loved it. Her team loved it. The investors loved the demo. The first customers loved the results.&lt;/p&gt;
&lt;p&gt;The problems started six months in, when Elena tried to take the prototype to production.&lt;/p&gt;
&lt;p&gt;The first problem was customization. CrewAI&amp;rsquo;s high-level abstractions — roles, goals, backstories — made it easy to define agents but hard to control their behavior at a fine-grained level. Elena wanted to add a step between the researcher and the writer: a check against the customer&amp;rsquo;s account status that determined which response template to use. In LangGraph, this would have been a node in the graph with conditional edges. In CrewAI, it required reaching inside the framework&amp;rsquo;s agent-communication layer and injecting custom logic in a place the framework hadn&amp;rsquo;t designed for customization. The workaround was brittle and broke when CrewAI released a new version.&lt;/p&gt;
&lt;p&gt;The second problem was observability. When a customer complained about a wrong answer, Elena&amp;rsquo;s team needed to understand what had happened: which agent had produced the wrong information, what documents it had consulted, what reasoning had led to the error. CrewAI&amp;rsquo;s multi-agent communication was largely opaque. Elena&amp;rsquo;s engineers wrote custom logging to capture the inter-agent messages, but the logging was incomplete and inconsistent.&lt;/p&gt;
&lt;p&gt;The third problem was durability. CrewAI had no checkpointing. If a multi-agent session failed midway through — and with multiple agents making multiple model calls, failures were common — the entire session had to restart from the beginning. Elena&amp;rsquo;s agents were processing customer support tickets for e-commerce companies, and each ticket took roughly 30-90 seconds to process. A failure rate of 15% meant that 15% of tickets were processed twice, at twice the cost, with twice the latency. The economics were painful.&lt;/p&gt;
&lt;p&gt;Sam and Elena talked about this on a call in the spring of 2024.&lt;/p&gt;
&lt;p&gt;&amp;ldquo;You should look at LangGraph,&amp;rdquo; Sam said.&lt;/p&gt;
&lt;p&gt;&amp;ldquo;I know,&amp;rdquo; Elena said. &amp;ldquo;But migrating would take three months minimum. We&amp;rsquo;d have to rewrite the agent logic, the inter-agent communication, the prompt templates, the evaluation suite. And we&amp;rsquo;d ship nothing new during those three months. The board would not be thrilled.&amp;rdquo;&lt;/p&gt;
&lt;p&gt;&amp;ldquo;What&amp;rsquo;s the alternative?&amp;rdquo;&lt;/p&gt;
&lt;p&gt;&amp;ldquo;Keep patching CrewAI. Add our own checkpointing on top. Build better logging.&amp;rdquo;&lt;/p&gt;
&lt;p&gt;&amp;ldquo;That&amp;rsquo;s building a state machine on top of a framework that wasn&amp;rsquo;t designed for state machines.&amp;rdquo;&lt;/p&gt;
&lt;p&gt;&amp;ldquo;Yes, Sam, I know what it is. I just don&amp;rsquo;t have a better option right now.&amp;rdquo;&lt;/p&gt;
&lt;p&gt;Sam recognized the trap. Elena was stuck on a framework whose low floor (easy to start) came with a low ceiling (hard to scale), and the cost of migrating to a higher-ceiling framework was prohibitive because she&amp;rsquo;d already built six months of product on the lower one. This was the framework selection problem in its purest form: the framework that was easiest to start with was rarely the one that was easiest to operate.&lt;/p&gt;
&lt;p&gt;&amp;ldquo;For what it&amp;rsquo;s worth,&amp;rdquo; Sam said, &amp;ldquo;I think you&amp;rsquo;ll end up migrating anyway. The question is whether you do it now, when it&amp;rsquo;s expensive, or later, when it&amp;rsquo;s more expensive.&amp;rdquo;&lt;/p&gt;
&lt;p&gt;Elena didn&amp;rsquo;t argue. She knew he was right. She also knew that &amp;ldquo;later&amp;rdquo; was the only option her board would accept.&lt;/p&gt;
&lt;hr&gt;
&lt;p&gt;While Sam was migrating to LangGraph and Elena was patching CrewAI, a team at OpenAI was approaching the entire problem from the opposite direction.&lt;/p&gt;
&lt;p&gt;Where LangGraph gave developers maximal control — every node, every edge, every state transition defined in code — the OpenAI team was drawn to maximal simplicity. They had observed that the most impressive agent behaviors often emerged not from elaborate orchestration but from a tight loop: give the model a task and some tools, let it decide what to do, observe the result, repeat until done. No graph. No state machine. Just a &lt;strong&gt;loop&lt;/strong&gt;.&lt;/p&gt;
&lt;p&gt;In October 2024, they published this idea as &lt;strong&gt;Swarm&lt;/strong&gt; — a framework so minimal that they labeled it &amp;ldquo;educational&amp;rdquo; and explicitly said it was not production-ready. Swarm had two abstractions: an &lt;strong&gt;Agent&lt;/strong&gt; (a model with instructions and tools) and a &lt;strong&gt;Handoff&lt;/strong&gt; (the ability for one agent to transfer control to another). No checkpointing. No durable execution. No streaming infrastructure. No human-in-the-loop.&lt;/p&gt;
&lt;p&gt;The loop metaphor assumed something radical about the model: that it was smart enough to manage its own execution. Rather than the developer prescribing a graph of possible paths, the model would reason about the task, decide what tool to use, evaluate the result, and determine the next step. The developer&amp;rsquo;s job was not to design the agent&amp;rsquo;s logic but to provide the right tools and get out of the way.&lt;/p&gt;
&lt;p&gt;Sam read the Swarm source code the weekend it was released. It was elegant and tiny — the core loop fit on a single screen. He showed it to Marcus on a call.&lt;/p&gt;
&lt;p&gt;&amp;ldquo;It&amp;rsquo;s beautiful,&amp;rdquo; Marcus said. &amp;ldquo;It&amp;rsquo;s also terrifying.&amp;rdquo;&lt;/p&gt;
&lt;p&gt;&amp;ldquo;Why terrifying?&amp;rdquo;&lt;/p&gt;
&lt;p&gt;&amp;ldquo;Because there are no guardrails. The model decides everything. If the model decides wrong — calls the wrong tool, accesses the wrong data, enters an infinite loop — there&amp;rsquo;s nothing to stop it. No checkpoints to roll back to. No approval gates. No audit trail. Just a model with tools, running unsupervised.&amp;rdquo;&lt;/p&gt;
&lt;p&gt;&amp;ldquo;It&amp;rsquo;s educational,&amp;rdquo; Sam said. &amp;ldquo;They said it&amp;rsquo;s not for production.&amp;rdquo;&lt;/p&gt;
&lt;p&gt;&amp;ldquo;People will use it for production anyway. They always do.&amp;rdquo;&lt;/p&gt;
&lt;p&gt;Marcus was right. Within weeks, developers were deploying Swarm-based agents in production environments, drawn by its simplicity and undeterred by the warnings. Some of them got burned. Others got lucky. The line between the two was thinner than anyone was comfortable with.&lt;/p&gt;
&lt;p&gt;Five months later, in March 2025, OpenAI released the &lt;strong&gt;Agents SDK&lt;/strong&gt; — the production-ready evolution of Swarm. It kept the minimalist philosophy (four primitives: Agents, Handoffs, Guardrails, Tracing) while adding the bare minimum of production infrastructure. Guardrails could validate inputs and outputs. Tracing captured the execution trajectory for debugging. But the SDK still had no checkpointing, no durable state, no human-in-the-loop. Persistence was the developer&amp;rsquo;s problem.&lt;/p&gt;
&lt;p&gt;The Agents SDK embodied a belief that Sam found both appealing and disquieting: as models got smarter, the harness could get thinner. A sufficiently capable model didn&amp;rsquo;t need a complex graph to guide its reasoning. It needed tools, constraints, and the freedom to work.&lt;/p&gt;
&lt;p&gt;Sam discussed this with David Almeida over video call.&lt;/p&gt;
&lt;p&gt;&amp;ldquo;The OpenAI position is that the model is smart enough to be trusted with the loop,&amp;rdquo; Sam said. &amp;ldquo;The LangGraph position is that no model is smart enough to be trusted without structure. Which is right?&amp;rdquo;&lt;/p&gt;
&lt;p&gt;David gave an answer that stuck with Sam for months: &amp;ldquo;They&amp;rsquo;re both right, for different tasks. The question isn&amp;rsquo;t &amp;lsquo;how smart is the model?&amp;rsquo; The question is &amp;lsquo;what happens if the model is wrong?&amp;rsquo; If the cost of being wrong is a bad draft that gets revised, use a loop. If the cost of being wrong is a financial transaction that can&amp;rsquo;t be reversed, use a graph with approval gates. The architecture should match the stakes, not the model&amp;rsquo;s capability.&amp;rdquo;&lt;/p&gt;
&lt;p&gt;Sam wrote this down. It would become one of the core principles of his team&amp;rsquo;s agent design: &lt;strong&gt;match the autonomy to the stakes&lt;/strong&gt;.&lt;/p&gt;
&lt;p&gt;The three metaphors — chain, graph, loop — were not competing answers to the same question. They were different answers to different questions, and the art of agent architecture was knowing which question you were asking.&lt;/p&gt;
&lt;hr&gt;
&lt;p&gt;There is one more development from this period that deserves attention. In November 2025, LangChain released version 1.0 alongside LangGraph 1.0. The release included a new &lt;code&gt;create_agent&lt;/code&gt; function — a high-level abstraction for building agents that was built, under the hood, on LangGraph. The old chains, agents, and memory modules were moved to a separate package called &lt;code&gt;langchain-classic&lt;/code&gt;. The message was unmistakable: the chain era was over.&lt;/p&gt;
&lt;p&gt;More striking was a project that appeared shortly afterward: &lt;strong&gt;DeepAgents&lt;/strong&gt;, an agent harness built with LangChain and LangGraph that the team described as having been &amp;ldquo;primarily inspired by Claude Code.&amp;rdquo; It followed what the creators called a &amp;ldquo;trust the LLM&amp;rdquo; philosophy: give the agent tools, enforce boundaries at the tool and sandbox level, and let the model drive. This was the loop philosophy — but implemented on top of graph infrastructure that provided the checkpointing and observability the pure loop lacked.&lt;/p&gt;
&lt;p&gt;Sam noticed the convergence. The team that built the chain had replaced it with the graph, then shipped a high-level agent that looked like a loop with guardrails. The team that started with a pure loop (OpenAI&amp;rsquo;s Swarm) was gradually adding structure. And the team that started with a fully equipped loop (Anthropic&amp;rsquo;s Claude Code) was exposing its architecture as a library. Three starting points. Three philosophies. And a gradual convergence toward a common architecture: a model-driven loop, running inside a graph that provided structure and checkpoints, with tool-level and sandbox-level enforcement replacing prescriptive orchestration.&lt;/p&gt;
&lt;p&gt;&amp;ldquo;Everyone&amp;rsquo;s building the same thing,&amp;rdquo; Sam told Elena on a call. &amp;ldquo;They just started from different corners of the design space.&amp;rdquo;&lt;/p&gt;
&lt;p&gt;&amp;ldquo;Great,&amp;rdquo; Elena said. &amp;ldquo;So which corner should I migrate to?&amp;rdquo;&lt;/p&gt;
&lt;p&gt;&amp;ldquo;The one that gives you checkpointing, human-in-the-loop, and observability. Because those are the things you&amp;rsquo;re going to wish you had when something goes wrong.&amp;rdquo;&lt;/p&gt;
&lt;p&gt;&amp;ldquo;You keep saying &amp;lsquo;when,&amp;rsquo; not &amp;lsquo;if.&amp;rsquo;&amp;rdquo;&lt;/p&gt;
&lt;p&gt;&amp;ldquo;I do,&amp;rdquo; Sam said. &amp;ldquo;Because I&amp;rsquo;ve talked to Marcus.&amp;rdquo;&lt;/p&gt;
&lt;hr&gt;
&lt;h2 id="act-2-the-architecture"&gt;&lt;a href="#act-2-the-architecture" class="header-anchor"&gt;&lt;/a&gt;Act 2: The Architecture
&lt;/h2&gt;&lt;p&gt;The three metaphors correspond to distinct execution models, and understanding them at a technical level clarifies both their strengths and their failure modes.&lt;/p&gt;
&lt;h3 id="the-chain-execution-model"&gt;&lt;a href="#the-chain-execution-model" class="header-anchor"&gt;&lt;/a&gt;The Chain Execution Model
&lt;/h3&gt;&lt;p&gt;A chain is a directed acyclic sequence of callable units. Each unit receives an input dictionary, performs its work, and returns an output dictionary that becomes the input to the next unit. The execution model is linear: call unit one, wait, pass to unit two, wait, pass to unit three.&lt;/p&gt;
&lt;p&gt;There is no branching in the execution engine. Branching must be implemented by the developer inside a unit&amp;rsquo;s code, which means the framework has no visibility into conditional paths. If a unit needs to retry on failure, the unit must implement its own retry logic.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;State management&lt;/strong&gt; is the chain&amp;rsquo;s most significant limitation. Input-output dictionaries are the only mechanism for passing data between steps. If step three needs information from step one that isn&amp;rsquo;t in step two&amp;rsquo;s output, the developer must ensure step two passes it through — threading state by hand. For simple chains, this is trivial. For chains with ten or twenty steps, it&amp;rsquo;s an exercise in defensive plumbing.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Error handling&lt;/strong&gt; is manual. If step five fails, the entire chain fails. There is no built-in mechanism for catching the error, reverting to a prior state, and trying an alternative path. The developer can wrap the chain in try-catch and restart, but restarting a chain that has already sent emails, modified records, or called external APIs creates obvious problems.&lt;/p&gt;
&lt;p&gt;Chains remain the right abstraction for well-defined workflows where the sequence is fixed and each step is idempotent. The mistake is reaching for a chain-like abstraction when the task requires branching, looping, or recovery.&lt;/p&gt;
&lt;h3 id="the-graph-execution-model"&gt;&lt;a href="#the-graph-execution-model" class="header-anchor"&gt;&lt;/a&gt;The Graph Execution Model
&lt;/h3&gt;&lt;p&gt;LangGraph replaced the chain with a &lt;strong&gt;StateGraph&lt;/strong&gt;: a directed graph where nodes are functions and edges define control flow.&lt;/p&gt;
&lt;p&gt;A LangGraph agent begins with a &lt;strong&gt;state definition&lt;/strong&gt; — a typed data structure representing everything the agent needs to know. Every node received the current state, did its work, and returned a partial update. The framework merged the update into the state using configurable reducers.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Edges&lt;/strong&gt; can be unconditional (always go from A to B) or &lt;strong&gt;conditional&lt;/strong&gt; (examine the state and route to different nodes). The typical agent pattern is: an &amp;ldquo;agent node&amp;rdquo; calls the model, a conditional edge routes to a &amp;ldquo;tool execution node&amp;rdquo; if the model produced tool calls or to the END node if it produced a final response, and the tool node routes back to the agent node. This cycle is the agent loop expressed as a graph.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Checkpointing&lt;/strong&gt; is implemented through a pluggable checkpointer interface. Before or after each node execution, the framework serializes the state and saves it to a backing store — SQLite for development, PostgreSQL or Redis for production. If the process fails, a new process loads the latest checkpoint and resumes.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Human-in-the-loop&lt;/strong&gt; is implemented as an &lt;strong&gt;interrupt&lt;/strong&gt;. The developer marks certain nodes as interrupt points. When the graph reaches one, it serializes the state, halts, and emits the state to the caller. From the graph&amp;rsquo;s perspective, the human&amp;rsquo;s input is just another state transition. The architecture doesn&amp;rsquo;t distinguish between model decisions and human decisions — both are state updates.&lt;/p&gt;
&lt;p&gt;The graph&amp;rsquo;s deepest insight is that checkpointing, HITL, error recovery, and streaming are all consequences of the same design: explicit state, deterministic transitions, and serializable execution. The graph is not just an orchestration pattern. It is a runtime.&lt;/p&gt;
&lt;h3 id="the-loop-execution-model"&gt;&lt;a href="#the-loop-execution-model" class="header-anchor"&gt;&lt;/a&gt;The Loop Execution Model
&lt;/h3&gt;&lt;p&gt;The OpenAI Agents SDK implements the loop through a &lt;strong&gt;Runner&lt;/strong&gt; that executes a tight cycle: send messages to the model, check the response, either return (if the model produced a final output) or execute tool calls and loop.&lt;/p&gt;
&lt;p&gt;The Runner maintains a list of messages. At each iteration, it sends the messages plus the agent&amp;rsquo;s instructions and tool definitions to the model. The model responds with text (final answer) or tool calls. If tool calls, the Runner executes each one, appends results to the messages, and loops.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Handoffs&lt;/strong&gt; are special tool calls. When the model invokes a handoff, the Runner replaces the active agent with the target agent and continues. The new agent inherits the message history but has its own instructions and tools.&lt;/p&gt;
&lt;p&gt;What the SDK notably lacks — by design — is persistent state. The Runner doesn&amp;rsquo;t checkpoint. It doesn&amp;rsquo;t save to a database. The message list exists in memory and is lost when the process ends. This is a deliberate bet that most interactions are short enough for in-memory state.&lt;/p&gt;
&lt;h3 id="the-convergence-pattern"&gt;&lt;a href="#the-convergence-pattern" class="header-anchor"&gt;&lt;/a&gt;The Convergence Pattern
&lt;/h3&gt;&lt;p&gt;By early 2026, the boundaries between these models were blurring. LangChain 1.0 shipped &lt;code&gt;create_agent&lt;/code&gt; on top of LangGraph. DeepAgents built a Claude Code-inspired loop on LangGraph infrastructure. The OpenAI Agents SDK added session management and more sophisticated guardrails. Anthropic&amp;rsquo;s Claude Agent SDK exposed a fully-equipped model-driven loop with built-in tools.&lt;/p&gt;
&lt;p&gt;The converged architecture: a &lt;strong&gt;model-driven loop&lt;/strong&gt; running inside a &lt;strong&gt;stateful execution engine&lt;/strong&gt; (with checkpointing, streaming, and interrupt support) with &lt;strong&gt;tool-level and sandbox-level enforcement&lt;/strong&gt;. Loop provides autonomy. Graph provides durability. Sandbox provides safety.&lt;/p&gt;
&lt;h3 id="the-cost-architecture"&gt;&lt;a href="#the-cost-architecture" class="header-anchor"&gt;&lt;/a&gt;The Cost Architecture
&lt;/h3&gt;&lt;p&gt;The choice of execution model has direct, often unintuitive cost implications.&lt;/p&gt;
&lt;p&gt;A &lt;strong&gt;chain&lt;/strong&gt; costs a fixed number of model calls — one per step, determined at design time. Predictable and easy to budget.&lt;/p&gt;
&lt;p&gt;A &lt;strong&gt;graph&lt;/strong&gt; costs a variable number — depending on the path taken. A graph with retry loops ranges from one call (no retries) to the maximum retry count. Bounded but variable.&lt;/p&gt;
&lt;p&gt;A &lt;strong&gt;loop&lt;/strong&gt; is unbounded by default. Without a maximum iteration count or token budget, the cost is unpredictable.&lt;/p&gt;
&lt;p&gt;The most significant cost factor is &lt;strong&gt;context growth&lt;/strong&gt;. Each iteration sends the accumulated state as input. A ten-turn interaction where each turn adds 500 tokens sends 500 input tokens on turn one, 1,000 on turn two, 1,500 on turn three. Total input cost: not 10x but approximately 55x the first turn. This quadratic growth is the hidden tax of long-running agents.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Checkpointing&lt;/strong&gt; provides economic benefit beyond durability. Without it, a failed ten-step loop wastes the cost of all ten steps. With it, recovery costs only the steps after the last checkpoint. At Elena&amp;rsquo;s 15% failure rate and $14,000 monthly model spend, the waste was roughly $5,000 per month — more than enough to justify the engineering investment in checkpointing infrastructure.&lt;/p&gt;
&lt;p&gt;The broader principle: the infrastructure that feels like overhead — checkpointing, observability, durable execution — is not cost added on top of the agent. It is cost subtracted from the agent&amp;rsquo;s failure modes. In a field where the most expensive thing is not running an agent but re-running a failed one, the harness is not overhead. It is savings.&lt;/p&gt;
&lt;p&gt;Sam understood this. Elena would learn it the hard way. The lesson, when it came, would cost more than $5,000 a month.&lt;/p&gt;
&lt;hr&gt;
&lt;p&gt;&lt;em&gt;Next: &lt;a class="link" href="https://igniting.github.io/the-invisible-stack/book/chapter-03-the-tool-problem/" &gt;Chapter 3 — The Tool Problem&lt;/a&gt;&lt;/em&gt;&lt;/p&gt;</description></item></channel></rss>